import * as sinon from 'test/helpers/sinon';

export const newUsageChecker = sinon.stub();
export const refresh = sinon.stub();
export const init = sinon.stub();
export const getEnforcements = sinon.stub();
