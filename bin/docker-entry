#!/usr/bin/env node

require('@babel/polyfill');

var run = require('../tools/docker/entry-script');
run(process.argv.slice(2)).catch((e) => {
  process.stderr.write('Fatal ' + (e instanceof Error ? e.stack : e) + '\n');
  process.exit(1);
});
