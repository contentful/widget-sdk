#!/bin/bash
#
# Run all the tests required for CI

set -veo pipefail

pushd packages/client
  npm test -- --reporter dot
popd

tools/bin/validate-config.js config/*.json
NODE_ENV=production npm run test:once
npm run hint
