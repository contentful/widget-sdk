#!/bin/bash
#
# Installs system dependencies on Ubuntu 14.04.
# That is only 'unzip' right now

set -e

packages=()

if [[ ! -x $(which unzip) ]]; then
  packages+=(unzip)
fi

if [[ ${#packages[@]} -gt 0 ]]; then
  sudo apt-get install -y "${packages[@]}"
fi
