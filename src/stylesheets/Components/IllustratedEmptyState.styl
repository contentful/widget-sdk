.illustrated-empty-state {
  display: flex;
  top: 0; bottom: 0; left: 0; right: 0;
  flex-direction: column;
  align-items: center;
  z-index: 0;
  overflow-x: hidden;
  text-align: center;

  &__background {
    height: 250px;
    width: 100%;
  }

  &__content {
    width: 500px
  }
}