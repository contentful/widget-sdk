describe('DeleteUser', () => {
  it('should default to selecting the "other" reason', () => {});

  it('should close the modal if the cancel button is pressed', () => {});

  it('should submit the form with the reason key and empty details if no details are given', () => {});

  it('should submit the form with the reason key and details if details are given', () => {});

  it('should show a notification if there was an error while deleting the user from the API', () => {});

  it('should call cancelUser if deletion was successful', () => {});
});
