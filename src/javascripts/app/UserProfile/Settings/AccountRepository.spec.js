describe('AccountService', () => {
  describe('fetchUserData', () => {
    it('should call the userEndpoint with GET and query', () => {});
  });

  describe('updateUserData', () => {
    it('should call the userEndpoint with PUT, data, and version', () => {});
  });

  describe('deleteUserIdentityData', () => {
    it('should call the userEndpoint with DELETE with identity path and id', () => {});
  });

  describe('deleteUserAccount', () => {
    it('should call the userEndpoint with DELETE with user_cancellations path and data', () => {});
  });
});
