describe('UserProfile Settings index', () => {
  it('should call onReady as soon as the component renders', () => {});

  it('should fetch user data when the component renders', () => {});

  it('should render a loading indicator when the component is fetching user data', () => {});

  it('should render an error state if fetching errs', () => {});

  it('should render the account details and user deletion sections if fetching is successful and user is not SSO restricted', () => {});

  it('should only render account details section if user is SSO restricted', () => {});
});
