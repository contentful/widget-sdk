describe('UserEditModal', () => {
  it('should close if the user presses the cancel button', () => {});

  it('should disable the submit button when the modal first loads', () => {});

  it('should warn on blur when typing in a empty trimmed first name', () => {});

  it('should warn on blur when typing in a empty trimmed last name', () => {});

  it('should warn on blur when typing in a empty trimmed email', () => {});

  it('should show the current password input if the email has changed and the user has a password', () => {});

  it('should not show the current password input if the email has changed but the user has no password', () => {});

  it('should warn on blur if the password is empty', () => {});

  it('should submit if there are no errors on the form when submitting', () => {});

  it('should warn if the server returns an error with invalid name for the current_password path', () => {});

  it('should warn if the server returns an error with length name for the first_name path', () => {});

  it('should warn if the server returns an error with length name for the last_name path', () => {});

  it('should show an error notification if an unknown error occurs', () => {});

  it('should fire the onConfirm with the response data if successful', () => {});
});
