describe('SpacePlanItem', () => {
  it('should show a price if the plan price is not 0', () => {});

  it('should not show a price if the price is 0', () => {});

  it('should show the free space copy of the plan is free and there is a free space limit', () => {});

  it('should show the plan features', () => {});

  it('should show the chevron if the plan is not disabled', () => {});

  it('should call onSelect with the plan if not disabled', () => {});
});
