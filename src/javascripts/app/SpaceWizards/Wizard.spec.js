describe('Wizard', () => {
  it('should get the base plan and determine if the plan is enterprise if creating a space', () => {});

  it('should get the current space plan and determine if the space is POC if changing the space plan', () => {});

  it('should show the loading spinner while the data is being fetched', () => {});

  it('should show the on-demand space creation wizard if the base plan is not enterprise', () => {});

  it('should show the POC space creation wizard if the base plan is enterprise', () => {});

  it('should show the on-demand space change wizard if changing a space plan in a non-enterprise org', () => {});

  it('should show the space change warning if changing a POC or committed space', () => {});
});
