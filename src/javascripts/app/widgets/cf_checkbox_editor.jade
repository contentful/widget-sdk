react-component(
  ng-if="misconfigured"
  name="app/widgets/EditorWarningPredefinedValues.es6"
)

div.checkbox-editor__options.x--directed(
  ng-if="!misconfigured"
  ng-class="isDisabled && 'x--disabled'")
  label.checkbox-editor__option(
    ng-repeat="option in options track by option.id")
    input.checkbox-editor__input(
      type="checkbox"
      id="{{options.id}}"
      ng-model="option.selected"
      ng-disabled="isDisabled")
    span.checkbox-editor__label
      | {{ option.value }}
