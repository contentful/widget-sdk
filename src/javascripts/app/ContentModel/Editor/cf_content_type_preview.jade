div.ct-editor-json
  div(ng-if="isNew")
    div.note-box--info(style="margin-bottom: 2em")
      p
        | We will show you a preview once the content type has been saved.
        button.btn-inline(ui-command="actions.save")
          | Save now and get preview
    code.ct-editor-json__code--unsaved
      pre {{preview | json}}

  div(ng-if="!isNew")
    div.loader__container(ng-if="isLoading" style="height: 200px")
      .loader__spinner
      .loader_message
        | Loading JSON preview
    div(ng-show="!isLoading")
      div.note-box--info(
        style="margin-bottom: 2em"
        ng-show="contentTypeForm.$dirty")
        p
          | You have unsaved changes.
          button.btn-inline(ui-command="actions.save")
            | Save now and get preview
      code.ct-editor-json__code
        pre {{preview | json}}
