header.workbench-header
  h1.workbench-header__title(title="{{context.title}}") {{context.title}}
  .workbench-header__description(ng-if="locale.data.default")
    | Default locale
  .workbench-header__actions
    button.cfnext-btn-secondary-action(
      ui-command="localeEditor.cancel")
      | Cancel
    button.cfnext-btn-secondary-action(
      ui-command="localeEditor.delete")
      | Delete
    button.cfnext-btn-primary-action(
      type="submit"
      ui-command="localeEditor.save")
      | Save

.workbench-main.x--content
  form.workbench-main__middle-content(name="localeForm" submit="save()")
    .cfnext-form__field
      label Locale and Code
      select.cfnext-select-box(
          ng-model="locale.data.code"
          ng-options="item.code as (item.name + ' (' + item.code + ')') for item in locales")
        | Locale
        option(value="") Select a locale
    .cfnext-form__fieldset
      legend Options
      .cfnext-form-option
        input(id="cda" type="checkbox" ng-model="locale.data.contentDeliveryApi" ng-disabled="locale.data.default")
        div.cfnext-form-option__desc
          label(for="cda") Enable this locale in response
          p Includes locale in the Delivery API response
      .cfnext-form-option
        input(id="cma" type="checkbox" ng-model="locale.data.contentManagementApi" ng-disabled="locale.data.default")
        div.cfnext-form-option__desc
          label(for="cma") Enable editing for this locale
          p Displays locale to editors and enables it in Management API
      .cfnext-form-option
        input(id="optional" type="checkbox" ng-model="locale.data.optional" ng-disabled="locale.data.default")
        div.cfnext-form-option__desc
          label(for="optional") Allow empty fields for this locale
          p Entries with required fields can still be published if locale is empty
