div.entry-editor__input-group
  input(
    type="text"
    aria-invalid="{{hasErrors || state === 'duplicate'}}"
    ng-disabled="isDisabled")
  div.entry-editor__input-spinner(ng-show="state === 'checking'")
.cfnext-form__field-error(
  role="status"
  data-status-code="entry.slug.duplicate"
  ng-show="state === 'duplicate'")
  | This slug has already been published in another entry
