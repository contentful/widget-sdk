input.cfnext-form__input--full-size(
  type="text"
  ng-disabled="isDisabled")
.cfnext-form__field-error(
  role="status"
  data-status-code="entry.url.invalid"
  ng-show="urlStatus === 'invalid'")
  | Invalid URL
.cfnext-form__field-error(
  role="status"
  data-status-code="entry.url.broken"
  ng-show="urlStatus === 'broken'")
  | Unable to access URL
.cfnext-form__hint(
  role="note"
  data-test-id="field-hint")
  | {{ helpText }}

div(ng-show="urlStatus === 'loading'")
  .loading-box(style="height: 470px")
    .loading-box__spinner
    .loading-box__message
      | Preview will be ready in a few moments
div(ng-hide="urlStatus === 'loading'")
  cf-embedly-preview(preview-url="previewUrl" url-status="urlStatus")
