.add-dropdown__container.dropdown-btn(cf-dropdown)
  a.add-dropdown__toggle.dropdown-toggle(cf-dropdown-toggle tabindex="0")
    i.fa.fa-plus
  .add-dropdown__menu.dropdown-menu.fixed-position.left-aligned(cf-dropdown-menu)
    ul.add-dropdown__list
      li.dropdown-menu-item.add-content-type.bottom-separator(
          dropdown-close
          ng-click="entityCreationController.newContentType('addDropdown')"
          ng-hide="permissionController.get('createContentType', 'shouldHide')")
        i.fa.fa-database
        | Content Type
      li.dropdown-menu-item.add-asset(
          dropdown-close
          ng-click="entityCreationController.newAsset('addDropdown')"
          ng-hide="permissionController.get('createAsset', 'shouldHide')")
        i.fa.fa-paperclip
        | Asset
      li.dropdown-menu-item.add-api-key(
          ng-click="entityCreationController.newApiKey('addDropdown')"
          dropdown-close
          ng-hide="permissionController.get('createApiKey', 'shouldHide')")
        i.fa.fa-random
        | API Key
    ul.add-dropdown__list.separator(ng-hide="permissionController.get('createEntry', 'shouldHide')")
      li.dropdown-menu-item--no-highlight
        i.fa.fa-file
        | Entries
    ul.add-dropdown__list.add-dropdown__content-types(ng-hide="spaceContext.publishedContentTypes.length === 0 || permissionController.get('createEntry', 'shouldHide')")
      li.dropdown-menu-item(ng-repeat="(index, contentType) in spaceContext.publishedContentTypes track by contentType.getId()"
        ng-click="entityCreationController.newEntry(contentType, 'addDropdown')"
        data-content-type-id="{{contentType.getId()}}"
        dropdown-close)
        | {{contentType.getName()}}
    ul.add-dropdown__list.add-dropdown__no-content-types.highlight(ng-hide="spaceContext.publishedContentTypes.length > 0 || permissionController.get('createEntry', 'shouldHide')")
      li.dropdown-menu-item.hint(dropdown-close ng-hide="permissionController.get('createContentType', 'shouldHide')")

        | (Create and activate a Content Type before creating Entries)
      li.dropdown-menu-item.hint(dropdown-close ng-hide="!permissionController.get('createContentType', 'shouldHide')")
        | (No Content Types available. An admin needs to create a Content Type before you can add Entries)
