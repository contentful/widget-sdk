header.workbench-header
  cf-icon.workbench-header__icon(name="page-settings")
  h1.workbench-header__title Roles ({{ (roles.length || 0) + 1 }})

.workbench-main
  .workbench-main__content
    .entity-editor__notification(ng-if="!canModifyRoles()" role="alert")
      p Your plan does not include Custom Roles. You can still access predefined roles in a read-only mode.

    .role-list__item
      .role-list__link.admin-role
        h3
          | Administrator
          small (built-in)
        p Members of this role have a full access to everything in this space. This is a built-in role and can't be deleted.
      .role-list__member-count
        a(href="" ng-click="jumpToAdminRoleMembers()")
          | {{ memberships.admin || 0 }} members
      button.role-list__actions.role-list__actions--disabled.cfnext-btn-inline
        | &bullet;&bullet;&bullet;

    .role-list__item(ng-repeat="role in roles track by role.sys.id")
      a.role-list__link(href="" ui-sref="spaces.detail.settings.roles.detail({roleId: role.sys.id })")
        h3 {{ role.name }}
        p(ng-if="role.description") {{ role.description }}
      .role-list__member-count
        a(href="" ng-click="jumpToRoleMembers(role.sys.id)")
          | {{ memberships[role.sys.id] || 0 }} members
      button.role-list__actions.cfnext-btn-inline(type="button" aria-label="Actions" cf-context-menu-trigger)
        | &bullet;&bullet;&bullet;
      .context-menu(role="menu" cf-context-menu="bottom-right" aria-label="Role Actions")
        cf-icon.context-menu__arrow.x--top-right-tight(name="arrow-up")
        ul.context-menu__items
          li(role="menuitem" ui-sref="spaces.detail.settings.roles.detail({roleId: role.sys.id })") Open role
          li(ng-if="canModifyRoles()" role="menuitem" ng-click="duplicateRole(role)") Duplicate
          li(ng-if="canModifyRoles()" role="menuitem" ng-click="removeRole(role)") Delete permanently
          li.role-list__action--disabled(ng-if="!canModifyRoles()") Duplicate
          li.role-list__action--disabled(ng-if="!canModifyRoles()") Delete permanently

  .workbench-main__entity-sidebar
    .entity-sidebar(ng-if="canModifyRoles()")
      h2.entity-sidebar__heading
        | Create new role
      p
        | Select one of the predefined role templates or create one from the scratch.
      button.cfnext-btn-primary-action(ui-sref="spaces.detail.settings.roles.new")
        | Create new role
    .entity-sidebar(ng-if="!canModifyRoles()")
      h2.entity-sidebar__heading
        | Custom roles disabled
      p
        | Your plan does not include Custom Roles.
        | Creating, editing, duplicating and removing roles is&nbsp;disabled.
        | Please upgrade your plan to activate this feature.
