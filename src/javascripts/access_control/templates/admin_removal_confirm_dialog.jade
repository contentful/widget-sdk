.modal-background
  .modal-dialog
    header.modal-dialog__header
      h1 Remove user from a space
      button.modal-dialog__close(ng-click="dialog.cancel()")

    .modal-dialog__content
      .modal-dialog__richtext
        p
          | You are about to remove <span class="modal-dialog__highlight">{{ user.name }}</span> from the space.
          | {{ user.name }} is the last Administrator in this space. Please be aware that if you remove this user,
          | there will be no one who can fully control the space. This, however, can be managed from your Organization settings.
        p
          | If you definitely want to delete this user, please type "I UNDERSTAND" in the field below:
        input.cfnext-form__input--full-size(ng-model="input.confirm")

    .modal-dialog__controls
      button.cfnext-btn-caution(
        ng-click="dialog.confirm()"
        ng-disabled="input.confirm !== 'I UNDERSTAND'")
        | Delete
      button.cfnext-btn-secondary-action(ng-click="dialog.cancel()")
        | Don't delete
