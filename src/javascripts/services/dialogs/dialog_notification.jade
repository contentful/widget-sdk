.modal-background.x--position-top
  .notification-dialog
    .notification-dialog__message(ng-if="!isHtml" ng-bind="message")
    .notification-dialog__message(ng-if="isHtml" ng-bind-html="message")
    .notification-dialog__controls
      button.cfnext-btn-primary-action(ng-click="dialog.confirm()")
        | {{ confirmLabel }}
