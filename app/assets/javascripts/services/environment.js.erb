angular.module('contentful/services').provider('environment', function () {
  'use strict';

  var env = <%= File.read(Rails.root + 'config' + 'environments' + Rails.env + 'config.json') %>;
  this.env = env;

  this.$get = function () {
    return env;
  };
});
