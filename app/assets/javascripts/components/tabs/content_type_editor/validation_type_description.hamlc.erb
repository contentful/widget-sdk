%ng-switch(on="validationType(type)")
  %div(ng-switch-when="in")
    %h3 Predefined Value(s)
    %p
      Your content must match exactly one the values inserted here. Use commas to separate different values.
    %img(src="<%= image_path('validation_type/in.png') %>")

  %div(ng-switch-when="regexp")
    %h3 Regular Expression
    %p
      Add regular expressions to search, edit and manipulate strings.
    %img(src="<%= image_path('validation_type/regexp.png') %>")

  %div(ng-switch-when="size")
    %h3 Length
    %p
      Constrain your character count or number of elements by adding a minimum and/or maximum boundaries.
    %img(src="<%= image_path('validation_type/size_range.png') %>")


  %div(ng-switch-when="range")
    %h3 Numberical Range
    %p
      Constrain your field to an interval by adding a minimum and/or maximum value for that field.
    %img(src="<%= image_path('validation_type/size_range.png') %>")

  %div(ng-switch-when="linkContentType")
    %h3 Content Type
    %p
      Allow links only to Entries of a specific Content Type.
    %img(src="<%= image_path('validation_type/contenttype.png') %>")

  %div(ng-switch-when="linkMimetypeGroup")
    %h3 File Type
    %p
      Define the file type your linked Assets should have. 
    %img(src="<%= image_path('validation_type/mimetype.png') %>")
