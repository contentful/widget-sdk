%ul.search-autocomplete-list
  %li(ng-repeat="(index, item) in autocompletion.items" ng-class="{selected: item.value === selectedAutocompletion.value}")
    %span.item-value {{item.value}}
    %span.item-description {{item.description}}
